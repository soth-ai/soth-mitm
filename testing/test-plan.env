# Config for scripts/run_testing_plan.sh
# Comma or space separated values.
TEST_PROFILES="stress parity benchmark"
# Add `soak` explicitly when running the long-duration Phase-5 runtime lane.
# `stress` includes `phase5_reliability_contract`.

# Optional explicit lane selection (comma or space separated lane IDs).
# If set, TEST_PROFILES is ignored and only listed lanes run.
TEST_LANES=""

# Output root for this run. Empty means timestamped default under artifacts/testing-plan.
REPORT_DIR=""

# Runtime toggles
STRICT_TOOLS=0
SKIP_NETWORK=1
ENFORCE_FAULTS=0
FAIL_FAST=1

# Soak gate policy: set to 1 to enforce strict launch-gate checks for soak lanes.
SOAK_STRICT_GATE=0
